# Context

本项目的词汇表。只写「这个词指什么」，不写实现。

## Origin（原点 / ME）

玩家自己的炮位坐标。一次设定，之后一直沿用 —— 迫击炮架好就不挪，
所以第一次复制到的坐标是 Origin，后面每次复制都不是。
只有 RESET 能换掉它。

## Target（目标 / TGT）

要打的那个点。Origin 已有之后，每一次复制到的坐标都是新的 Target，
直接顶掉上一个。同一时刻只有一个 Target。

## Firing Preset（面板上的「炮种」下拉）

玩家一次选定的「我在打什么」：`MORTAR` / `SPG · LOW` / `SPG · HIGH`。
它是 UI 层的概念，把 Weapon 与 Arc 合成一个选择，因为玩家心里只有一个念头。
core 里这两者仍然正交，不要把 Preset 的概念带进 core。

## Weapon（武器）

火表里的一门炮：id、最小/最大射程、各弧的「距离 → 密位」采样点。
目前只有 `mortar`（L81）与 `spg`（SPH-2）。

## Arc（弹道弧）

同一门炮打同一距离的不同打法：`Single`（迫击炮唯一一条）、`Low`、`High`（SPG 两条）。
低角与高角在最大射程附近收敛，那一段同一距离对应多个密位。

## Mil（密位）

炮的仰角读数，玩家在游戏 HUD 的密位带上对齐的那个数。
火表给不出确定值的那一段是区间，不是单值 —— 区间就照区间显示。

## Solution（射击诸元）

由 Origin + Target + Weapon 推出来的一组读数：距离、方位角、各弧密位、是否在射程内。
它是纯推导物，输入一变就整体重算，没有「部分失效」这种状态。

## Ghost Ticks（幽灵刻度）

画在游戏画面上的目标读数：密位带上四条刻度线 + 右侧数字，以及方位角罗盘条上的标记。
玩家转炮/调仰角，直到游戏自己的刻度与幽灵刻度重合。
它永远等于当前 Solution，没有 Solution、超出射程、或被 CLEAR OVL 静音时必须是空的 ——
屏幕上留着上一个目标的幽灵刻度是本工具最危险的失效方式。

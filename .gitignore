
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# 任意层级的构建产物（app/ 是独立 workspace，有自己的 target/）
target/

# T1 探针输出的截图，不入库
win/capture-frame-*.png

# 游戏截图（大文件 + 游戏内容，不入库）
Wardogs Screenshot*.png
